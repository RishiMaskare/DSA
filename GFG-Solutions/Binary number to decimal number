class Solution {
  public:
    int binaryToDecimal(string &b) {
        // Code here.
        int ans = 0;
        int pow = 1;
        for(int i=b.size()-1;i>=0;i--) {
            ans += (b[i]-'0') * pow;
            pow *= 2;
        }
        return ans;
    }
};
